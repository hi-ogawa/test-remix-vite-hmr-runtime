name: ci
"on":
  - push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # - uses: pnpm/action-setup@v2
      #   with:
      #     version: 8
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          # cache: pnpm
      - run: npm i -g @antfu/ni
      - run: ni
      - run: nlx playwright install chromium
      - run: nr test-e2e
